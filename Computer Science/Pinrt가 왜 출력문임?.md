## Print
```
출력문이다.

근데 뭘 출력함?

먼저 하드웨어관점에서 보자

우리는 모니터와 키보드를 사용하여 코드를 짜게된다.

본체는 CPU랑 메모리가 우리가 짠 멍청한 코드를 실행할 때

중간에 System.out.print 이걸 만나게 된다.




키보드는 입력장치이고 모니터는 출력장치이다.

System.out.print 는 어디서 왔을까?

하드웨어에서 왔기 때문에

모니터로 출력해 = out.print를 쓰는거고

키보드에서 입력받아 = Scanner(System.in)를 쓰는거다.



정확한 설명은 아니지만 이렇게 설명하는게 제일 쉬울 것 같았다.

솔직히 그렇게 틀린것도 아닌데 이걸 기술면접 때 물어보는 사람이 있을까?
```

## print printf println
```
print는 write를 기반으로 만들어졌다.

list가 array를 기반으로 만들어졌듯이.

print를 기반으로 printf, println가 만들어졌다.

실제 구현 코드를 보면 되게 간단하다.(굳이 안봐도 되지만)


그래서 write가 뭐에요. > 힙스터가 쓰는 메서드라고 생각하면 된다.
```
